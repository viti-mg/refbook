// Auth client configuration
// This will be expanded when we implement auth routes and server functions
// For now, we're setting up the import structure from @packages/auth

import type { auth } from '@packages/auth';

export type Auth = typeof auth;
